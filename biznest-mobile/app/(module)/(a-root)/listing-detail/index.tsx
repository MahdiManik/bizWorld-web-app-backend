import React from 'react';
import ListingDetail from '@/feature/(modules)/listing/components/ListingDetail';

const ListingDetailScreen = () => {
  return <ListingDetail />;
};

export default ListingDetailScreen;
