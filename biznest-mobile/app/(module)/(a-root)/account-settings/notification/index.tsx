import React from 'react';
import Notifications from '@/feature/(modules)/account-settings/notification/Notifications';

const NotificationScreen = () => {
  return <Notifications />;
};

export default NotificationScreen;
